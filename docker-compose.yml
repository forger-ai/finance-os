services:
  backend:
    extends:
      file: ./commons/docker-compose.base.yml
      service: backend
    build:
      context: ./backend
      dockerfile: ../commons/backend/Dockerfile
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: sqlite:////app/data/finance_os.dev.sqlite
      CORS_ORIGINS: http://localhost:5180,http://127.0.0.1:5180
    volumes:
      - ./backend:/app
      - ./backend/data:/app/data
      - ./commons/backend/database.py:/app/app/database.py
      - ./commons/backend/health.py:/app/app/health.py
      - ./commons/backend/cors.py:/app/app/cors.py

  frontend:
    extends:
      file: ./commons/docker-compose.base.yml
      service: frontend
    build:
      context: ./frontend
      dockerfile: ../commons/frontend/Dockerfile
    ports:
      - "5180:5180"
    environment:
      VITE_API_BASE_URL: http://localhost:8000
    volumes:
      - ./frontend:/app
      - frontend_node_modules:/app/node_modules
      - ./commons/frontend/client.ts:/app/src/api/client.ts
    depends_on:
      - backend

volumes:
  frontend_node_modules:
